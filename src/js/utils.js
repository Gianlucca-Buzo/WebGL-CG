//Movimentos
const degToRad = (d) => (d * Math.PI) / 180;
const rotacaoEixoX = (f) => (f * Math.PI) / 180;

const translacaoX = (tx) => (tx);
const translacaoY = (ty) => (ty);

const teste = (t) => (t);


//Camera
const zoom = (r) => (r);
const rotacaoCameraX = (rcx) => (rcx);
const rotacaoCameraY = (rcy) => (rcy);
// const radToDeg = (r) => (r * 180) / Math.PI;


